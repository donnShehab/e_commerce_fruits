import 'package:flutter/material.dart';

abstract class AppColors {
  // Brand Colors
  static const Color primaryColor = Color(0xFFF57C00); // Orange
  static const Color lightPrimaryColor = Color(0xFFFFA726); // Lighter Orange

  static const Color secondryColor = Color(0xFF2E7D32); // Green
  static const Color lightSecondryColor = Color(0xFF66BB6A); // Lighter Green

  // Semantic Colors
  static const Color successColor = Color(0xFF2E7D32);
  static const Color errorColor = Color(0xFFD32F2F);

  // Neutral Colors
  static const Color backgroundColor = Color(0xFFFFFFFF);
  static const Color cardColor = Color(0xFFFFFFFF);

  static const Color primaryTextColor = Color(0xFF1A1A1A);
  static const Color secondaryTextColor = Color(0xFF757575);

  static const Color borderColor = Color(0xFFE0E0E0);
}
